# JS Проект «Kekstagram»
---

## 📸 Сервис просмотра и загрузки изображений

**Kekstagram** — это веб-приложение для просмотра фотографий других пользователей и загрузки собственных изображений с возможностью редактирования и применения эффектов.

---

## 🛠 Используемые технологии

- JavaScript (ES6+)
- Работа с DOM и шаблонами
- Fetch API / AJAX
- noUiSlider
- Pristine
- CSS-фильтры и трансформации
- Модальные окна и управление состоянием интерфейса

---

## 🚀 Реализованная функциональность

### 1. Просмотр загруженных изображений

- Загрузка данных с сервера при старте приложения
- Отрисовка миниатюр на основе HTML-шаблона
- Обработка ошибок загрузки данных
- Открытие изображения в полноэкранном режиме
- Отображение:
  - Полноразмерного изображения
  - Количества лайков
  - Комментариев пользователей

#### Комментарии
- Первоначально отображается **не более 5 комментариев**
- Подгрузка комментариев по кнопке «Показать ещё»
- Обновление счётчика показанных комментариев
- Автоматическое скрытие кнопки загрузки, если комментарии закончились

---

### 2. Фильтрация изображений

- Реализованы фильтры:
  - **По умолчанию** — порядок сервера
  - **Случайные** — 10 случайных уникальных изображений
  - **Обсуждаемые** — по убыванию количества комментариев
- Блок фильтрации отображается после загрузки данных
- Использован **debounce (500 мс)** для оптимизации перерисовки

---

### 3. Загрузка и редактирование изображений

- Загрузка изображения через стандартный input, стилизованный под элемент интерфейса
- Предпросмотр загруженного изображения в форме редактирования
- Открытие и закрытие формы редактирования (кнопка X и клавиша `Esc`)
- Блокировка прокрутки страницы при открытых модальных окнах

#### Масштабирование изображения
- Изменение масштаба с шагом **25%**
- Диапазон масштабирования: **от 25% до 100%**

#### Применение эффектов
- Доступные эффекты:
  - Оригинал
  - Хром (`grayscale`)
  - Сепия (`sepia`)
  - Марвин (`invert`)
  - Фобос (`blur`)
  - Зной (`brightness`)

- Регулировка интенсивности эффекта с помощью **noUiSlider**
- Автоматический сброс параметров при смене эффекта
- Скрытие слайдера для эффекта «Оригинал»

---

### 4. Валидация данных формы

#### Хэш-теги
- Реализована пользовательская валидация с помощью **Pristine**
- Начинаются с `#`
- Содержат только буквы и цифры
- Проверка на длину: максимум — **20 символов**
- Проверка на количество: не более **5 хэш-тегов**
- Теги нечувствительны к регистру
- Запрещены дубликаты

#### Комментарий
- Поле необязательное
- Максимальная длина — **140 символов**
- Нажатие `Esc` в поле ввода не закрывает форму

---

### 5. Отправка данных на сервер

- Отправка формы с изображением и данными через **AJAX (POST, multipart/form-data)**
- Блокировка кнопки отправки на время запроса
- Обработка успешной отправки:
  - Закрытие формы
  - Сброс масштаба и эффектов
  - Очистка полей ввода и input загрузки файла
  - Показ сообщения об успехе
- Обработка ошибок запроса:
  - Показ сообщения об ошибке
  - Закрытие по кнопке, `Esc` и клику вне окна
